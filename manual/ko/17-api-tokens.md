# 제 4장: API 토큰 관리 (API Token Management)

외부 툴이나 스크립트가 Gatrix 시스템에 접근할 수 있도록 권한을 부여하는 'API 토큰'을 관리합니다.

**경로:** 관리 (Admin) → API 토큰 (API Tokens)  
**필요 권한:** `admin` 등급

---

## 토큰이 왜 필요한가요?

예를 들어, "빌드 서버(Jenkins)에서 클라이언트 빌드가 끝나면 자동으로 Gatrix에 새 버전을 등록하고 싶다"는 요구가 있을 수 있습니다.
이때 사람이 매번 로그인하는 대신, Jenkins에게 **'버전 등록 권한만 가진 열쇠(토큰)'**를 쥐여주는 것입니다.

## 토큰 생성하기

1. **[+ 토큰 생성]** 클릭
2. **이름:** 용도를 식별할 수 있는 이름 (예: `Jenkins Build Bot`)
3. **권한 범위 (Scopes):** 최소한의 권한만 선택
   - 예: `client_version:write`(버전 등록)만 체크하고 `user:delete` 등은 체크 해제
4. **[생성]** 클릭

> **⚠️ 중요:** 토큰 값은 **생성 직후 딱 한 번만** 보여줍니다. 다시는 볼 수 없으므로 안전한 곳에 즉시 복사해 두세요.

## 토큰 관리

- 해당 스크립트를 더 이상 안 쓰게 되거나, 토큰이 유출된 것 같다면 목록에서 즉시 **[삭제/Revoke]** 하세요. 해당 토큰은 즉시 무효화됩니다.

---

> **보안 팁:** API 토큰은 비밀번호와 같습니다. 소스 코드(Git)에 직접 적어두지 말고 환경 변수 등으로 안전하게 관리하세요.

---

**이전 장:** [← 제 3장: 사용자 관리](03-user-management.md)  
**다음 장:** [제 5장: 클라이언트 버전 관리 →](04-client-versions.md)
