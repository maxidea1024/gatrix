/**
 * PillSwitch – A pill-shaped toggle switch with optional label text inside.
 *
 * With `label`: pill track with sliding grip + truncated label text inside.
 * Without `label`: compact iOS-style toggle.
 *
 * @example
 *   <PillSwitch checked={on} onChange={() => setOn(!on)} label="범례 표시" size="small" />
 */
export { default } from './FeatureSwitch';
