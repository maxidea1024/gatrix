# ============================================
# Frontend Environment Variables
# ============================================
# Copy this file to .env.local for local development

# Docker Environment Flag
# Set to "true" when running in Docker, leave unset or "false" for local development
# This is automatically set in docker-compose.dev.yml
# DOCKER_ENV=false
# VITE_DOCKER_ENV=false

# Backend Configuration for Local Development
# When running `yarn dev` locally (not in Docker), the backend typically runs on port 5001
# The vite.config.ts uses this to configure the proxy target
BACKEND_PORT=5001

# API Configuration
# For local development (yarn dev): use port 5001
# For Docker development: use port 5000 (configured in docker-compose.dev.yml)
VITE_API_URL=http://localhost:5001/api/v1

# Default Language
VITE_DEFAULT_LANGUAGE=ko

# App Configuration
VITE_APP_NAME=Gatrix
VITE_APP_DESCRIPTION=Gatrix for UWO - Online Game Platform Management

# Router Configuration
# Set the base path for the router (e.g., "/" for root, "/admin/" for /admin subdirectory)
# This is useful when the app is deployed in a subdirectory
VITE_ROUTER_BASENAME=/

# ============================================
# Environment Detection Notes:
# ============================================
# - In Docker: backend runs on 'backend-dev:5000' (internal Docker network)
# - In Local: backend runs on 'localhost:5001' (or the port specified in BACKEND_PORT)
# - The vite.config.ts automatically detects the environment and configures the proxy accordingly
# - DOCKER_ENV is set to "true" in docker-compose.dev.yml
# - For local development, DOCKER_ENV should be unset or "false"
