## Files Containing This Flag

The following files were found to contain at least one variant of `{{FLAG_KEY}}`.
**Focus your analysis exclusively on these files.** Do not scan or modify any other files.

```
{{RELEVANT_FILES}}
```

If a file in this list turns out not to contain a real flag reference upon inspection
(e.g. it only appears in a comment that is not guarding any logic), skip it.
