# ============================================
# Gatrix Docker Swarm Deployment Configuration
# ============================================
# Copy this file to .env and update values for your environment.
#   cp .env.example .env
#
# [REQUIRED] items must be configured before deployment.
# [OPTIONAL] items have sensible defaults.

# ==============================================
# Deployment Version [REQUIRED]
# ==============================================
# Image tag to deploy (e.g. 1.0.0, latest)
GATRIX_VERSION=latest

# ==============================================
# Cloud Database (MySQL) [REQUIRED]
# ==============================================
# Enter your Cloud MySQL connection details.
DB_HOST=your-cloud-mysql-host.com
DB_PORT=3306
DB_NAME=gatrix
DB_USER=gatrix_user
DB_PASSWORD=your-secure-db-password

# ==============================================
# Cloud Redis [REQUIRED]
# ==============================================
# Enter your Cloud Redis connection details.
# IMPORTANT: Game servers also need access to this Redis instance
# for Pub/Sub communication (feature flags, config sync, etc.).
# Ensure the Redis security group allows connections from both
# Gatrix services AND game server IPs.
REDIS_HOST=your-cloud-redis-host.com
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# ==============================================
# Security Secrets [REQUIRED - must change in production]
# ==============================================
# Use ./generate-secrets.sh to generate secure random values.
JWT_SECRET=change-this-to-a-secure-random-string
JWT_REFRESH_SECRET=change-this-to-another-secure-random-string
SESSION_SECRET=change-this-to-yet-another-secure-random-string
EDGE_API_TOKEN=change-this-to-edge-api-token
EDGE_BYPASS_TOKEN=change-this-to-edge-bypass-token

# ==============================================
# Network Ports [OPTIONAL]
# ==============================================
# Cloud LB handles routing in production:
#   Cloud LB -> Edge (:3400)       for game clients
#   Cloud LB -> Frontend (:43000)  for admin UI
#   Cloud LB -> Backend (:45000)   for admin API
#
# Nginx is disabled by default. Enable it only for
# dev/staging environments without a Cloud LB.
NGINX_REPLICAS=0
HTTP_PORT=80
HTTPS_PORT=443

# Direct access ports (for debugging, use if needed)
# BACKEND_PORT=45000
# FRONTEND_PORT=43000

# ==============================================
# OAuth Configuration [OPTIONAL]
# ==============================================
# Google OAuth (leave empty if not used)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth (leave empty if not used)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ==============================================
# Grafana Monitoring [OPTIONAL]
# ==============================================
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your-secure-grafana-password
GRAFANA_DOMAIN=localhost
GRAFANA_TRUSTED_ORIGINS=*
GRAFANA_ALLOWED_ORIGINS=*
GRAFANA_PORT=3000
PROMETHEUS_PORT=9090

# ==============================================
# Application Settings [OPTIONAL]
# ==============================================
# Default language: ko (Korean), en (English), zh (Chinese)
DEFAULT_LANGUAGE=zh
VITE_APP_NAME=Gatrix
VITE_DEFAULT_LANGUAGE=zh

# ==============================================
# Admin User Seed [OPTIONAL]
# ==============================================
# Auto-created on first startup. Change password after first login!
ADMIN_EMAIL=admin@gatrix.com
ADMIN_PASSWORD=admin123
ADMIN_NAME=Administrator

# ==============================================
# Service Scaling (replica count) [OPTIONAL]
# ==============================================
BACKEND_REPLICAS=2
FRONTEND_REPLICAS=2
EDGE_REPLICAS=2

# ==============================================
# Edge Server Configuration [OPTIONAL]
# ==============================================
EDGE_LOG_LEVEL=info
EDGE_APPLICATION_NAME=edge-server
EDGE_SYNC_METHOD=polling
EDGE_CACHE_POLLING_INTERVAL_MS=60000
EDGE_ENVIRONMENTS=*
EDGE_FORCE_HTTPS=false

# Edge-specific Redis [REQUIRED]
# IMPORTANT: Docker Compose does NOT support nested ${} substitution,
# so these cannot default to REDIS_HOST/PORT automatically.
# Set these to the same values as Cloud Redis above, or override
# to use a separate Redis instance for Edge.
EDGE_REDIS_HOST=your-cloud-redis-host.com
EDGE_REDIS_PORT=6379
EDGE_REDIS_PASSWORD=your-redis-password
EDGE_REDIS_DB=0

# ==============================================
# Monitoring [OPTIONAL]
# ==============================================
PROM_RETENTION_TIME=14d
