apiVersion: 1

# Provision Prometheus as the default data source
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus.gatrix.local:9090
    isDefault: true
    editable: true
